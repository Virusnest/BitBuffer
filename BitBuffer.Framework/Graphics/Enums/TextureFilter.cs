namespace BitBuffer.Framework.Graphics;

public enum TextureFilter
{
    /// <summary>
    /// No filtering, nearest neighbor sampling
    /// </summary>
    Nearest,

    /// <summary>
    /// Linear filtering, smooth interpolation between pixels
    /// </summary>
    Linear,
}
